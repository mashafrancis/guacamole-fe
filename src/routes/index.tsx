// react libraries
import * as React from 'react';

// components
import HomePage from 'pages/HomePage';

const Routes = [
  {
    path: '/',
    name: 'Home',
    component: HomePage,
    exact: true,
  },
];

export default Routes;
