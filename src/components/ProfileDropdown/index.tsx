// react libraries
import ProfileDropdownProps from 'components/ProfileDropdown/interfaces';
import * as React from 'react';

export const ProfileDropdown: React.SFC<ProfileDropdownProps> = props => (
  <React.Fragment>
  </React.Fragment>
)
